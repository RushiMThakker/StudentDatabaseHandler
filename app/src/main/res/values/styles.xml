<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="HintTheme" parent="TextAppearance.AppCompat">
    <item name="android:textColor">@color/colorPrimary</item>
  </style>

  <!-- The default font for RecyclerView items is too small. The margin is a simple delimiter between the words. -->
  <style name="student_title">
    <item name="android:layout_width">match_parent</item>
    <item name="android:layout_height">26dp</item>
    <item name="android:textSize">24sp</item>
    <item name="android:textStyle">bold</item>
    <item name="android:layout_marginBottom">6dp</item>
    <item name="android:paddingLeft">8dp</item>
  </style>

  <!--                                  DIALOG STYLES                                            -->
  <style name="AppCompatAlertDialogStyle" parent="Base.Theme.AppCompat.Light.Dialog.FixedSize">
    <item name="android:windowBackground">@android:color/transparent</item>
  </style>

  <style name="ExplodeAnimationAlertDialog" parent="AppCompatAlertDialogStyle">
    <item name="android:windowAnimationStyle">@style/ExplodeInExplodeOutAnimation</item>
  </style>

  <style name="ExplodeFadeAnimationAlertDialog" parent="AppCompatAlertDialogStyle">
  <item name="android:windowAnimationStyle">@style/ExplodeFadeInExplodeFadeOutAnimation</item>
  </style>
  <!--                                  DIALOG STYLES                                            -->

  <!--                                  WINDOWS ANIMATION STYLES                                 -->
  <style name="ExplodeInExplodeOutAnimation">
    <item name="android:windowEnterAnimation">@anim/explode_in</item>
    <item name="android:windowExitAnimation">@anim/explode_out</item>
  </style>

  <style name="ExplodeFadeInExplodeFadeOutAnimation">
    <item name="android:windowEnterAnimation">@anim/explode_fade_in</item>
    <item name="android:windowExitAnimation">@anim/explode_fade_out</item>
  </style>
  <!--                                  WINDOWS ANIMATION STYLES                                 -->
</resources>
